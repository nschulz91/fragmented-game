{
  "name": "fragmented-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "smoke": "npm run build && node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
